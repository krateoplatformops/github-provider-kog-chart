apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  creationTimestamp: "2025-09-18T15:24:46Z"
  generation: 1
  name: workflows.github.ogen.krateo.io
  resourceVersion: "816199"
  uid: cda4b5b0-22f0-48c5-baa3-0dfddc25c9af
spec:
  conversion:
    strategy: None
  group: github.ogen.krateo.io
  names:
    categories:
    - workflow
    - restresources
    - rr
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          spec:
            properties:
              configurationRef:
                description: A reference to the Configuration CR that holds all the
                  needed configuration for this resource. OASGen Provider added this
                  field automatically.
                properties:
                  name:
                    type: string
                  namespace:
                    description: Namespace of the referenced Configuration CR. If
                      not provided, the same namespace will be used.
                    type: string
                required:
                - name
                type: object
              inputs:
                description: Input keys and values configured in the workflow file.
                  The maximum number of properties is 10. Any default properties configured
                  in the workflow file will be used when `inputs` are omitted.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              owner:
                description: 'PARAMETER: path - The account owner of the repository.
                  The name is not case sensitive.'
                type: string
              ref:
                description: The git reference for the workflow. The reference can
                  be a branch or tag name.
                type: string
              repo:
                description: 'PARAMETER: path - The name of the repository without
                  the `.git` extension. The name is not case sensitive.'
                type: string
              workflow_id:
                description: 'PARAMETER: path - The ID of the workflow. You can also
                  pass the workflow file name as a string.'
                type: string
            required:
            - configurationRef
            - owner
            - ref
            - repo
            - workflow_id
            type: object
          status:
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedObjectRef:
                properties:
                  apiVersion:
                    description: API version of the object.
                    type: string
                  kind:
                    description: Kind of the object.
                    type: string
                  name:
                    description: Name of the object.
                    type: string
                  namespace:
                    description: Namespace of the object.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    categories:
    - workflow
    - restresources
    - rr
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  conditions:
  - lastTransitionTime: "2025-09-18T15:24:46Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2025-09-18T15:24:46Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1alpha1
