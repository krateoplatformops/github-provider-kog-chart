apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: teamrepoes.github.ogen.krateo.io
spec:
  conversion:
    strategy: None
  group: github.ogen.krateo.io
  names:
    categories:
    - teamrepo
    - restresources
    - rr
    kind: TeamRepo
    listKind: TeamRepoList
    plural: teamrepoes
    singular: teamrepo
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          spec:
            properties:
              configurationRef:
                description: A reference to the Configuration CR that holds all the
                  needed configuration for this resource. OASGen Provider added this
                  field automatically.
                properties:
                  name:
                    type: string
                  namespace:
                    description: Namespace of the referenced Configuration CR. If
                      not provided, the same namespace will be used.
                    type: string
                required:
                - name
                type: object
              org:
                description: 'PARAMETER: path - The organization name. The name is
                  not case sensitive.'
                type: string
              owner:
                description: 'PARAMETER: path - The account owner of the repository.
                  The name is not case sensitive.'
                type: string
              permission:
                description: 'The permission to grant the team on this repository.
                  We accept the following permissions to be set: `pull`, `triage`,
                  `push`, `maintain`, `admin` and you can also specify a custom repository
                  role name, if the owning organization has defined any. If no permission
                  is specified, the team''s `permission` attribute will be used to
                  determine what permission to grant the team on this repository.'
                type: string
              repo:
                description: 'PARAMETER: path - The name of the repository without
                  the `.git` extension. The name is not case sensitive.'
                type: string
              team_slug:
                description: 'PARAMETER: path - The slug of the team name.'
                type: string
            required:
            - configurationRef
            - org
            - owner
            - repo
            - team_slug
            type: object
          status:
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedObjectRef:
                properties:
                  apiVersion:
                    description: API version of the object.
                    type: string
                  kind:
                    description: Kind of the object.
                    type: string
                  name:
                    description: Name of the object.
                    type: string
                  namespace:
                    description: Namespace of the object.
                    type: string
                type: object
              full_name:
                type: string
              id:
                type: integer
              name:
                type: string
              permission:
                description: Added
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

