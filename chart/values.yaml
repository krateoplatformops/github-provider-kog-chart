webservice:
  service:
    port: 8080
  image:
    repository: leovice/github-rest-dynamic-controller-plugin
    tag: dev
    imagePullPolicy: Always
    ports:
      containerPort: 8080

restdefinitions:
  collaborator:
    enabled: false
  repo:
    enabled: false
  teamrepo:
    enabled: false
  workflow:
    enabled: true
